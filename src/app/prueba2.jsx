"use client";
import React from "react";

const Prueba2 = (props) => {
    // Recibe los datos pasados como props
    const { datos } = datos.props;

    return (
        <div>
            <p>{datos}</p>
        </div>
    );
};

export default Prueba2;
